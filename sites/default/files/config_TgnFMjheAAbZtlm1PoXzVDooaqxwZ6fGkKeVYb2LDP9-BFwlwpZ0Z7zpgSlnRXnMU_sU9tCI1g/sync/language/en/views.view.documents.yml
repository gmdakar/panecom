display:
  default:
    display_options:
      pager:
        options:
          tags:
            first: '« First'
            last: 'Last »'
      exposed_form:
        options:
          reset_button_label: Reset
      empty:
        area:
          content:
            value: "<p>No result.</p>\r\n"
            format: basic_html
      filters:
        combine:
          expose:
            description: 'Ex. title'
            placeholder: 'Search a document'
      fields:
        field_document_fichier:
          settings:
            link_text: 'Download ([node:field_document_fichier:entity:extension] - [file:size])'
