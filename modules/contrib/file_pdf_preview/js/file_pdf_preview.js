/**
 * @file
 * File PDF Preview.
 */

(function ($, Drupal, drupalSettings) {

  'use strict';

  Drupal.filePdfPreview = Drupal.filePdfPreview || {};

  Drupal.behaviors.file_pdf_preview = {
    attach: function (context, settings) {

    }
  };

})(jQuery, Drupal, drupalSettings);
